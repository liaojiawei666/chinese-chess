[workspace]
resolver = "2"
members = [
    "engine",
    "datagen",
    "play_gui",
]
[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"

[workspace.dependencies]
engine = { path = "engine" }
rand = { version = "0.8", features = ["small_rng"] }
rand_distr = "0.4"
